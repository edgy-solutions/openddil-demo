# Edge-02 → HQ bridge (ADR-0023 Phase 6a).
input:
  kafka:
    addresses:
      - redpanda-edge-02:9092
    topics:
      - raw-sensor-stream
      - tactical-events
    consumer_group: "bridge-group-edge-02"

output:
  kafka:
    addresses:
      - toxiproxy:8474
    topic: "${! meta(\"kafka_topic\") }"
    max_retries: 0
